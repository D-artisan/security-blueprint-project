# Security Blueprint — Local Development Credentials
# Copy this to .env and fill in your values:
#   cp .env.example .env

# PostgreSQL
POSTGRES_USER=scanner
POSTGRES_PASSWORD=changeme
POSTGRES_DB=appdb

# Grafana
GF_SECURITY_ADMIN_PASSWORD=changeme
